# Shopify Product field (left) -> Myntra Sarees header (right). Direct copies only.
# Note: Prominent Colour, Saree/Blouse Fabric, and Product Details are NOT mapped
# here — they are derived/transformed by rules (see config/rules.yaml).
title: vendorArticleName
sku: vendorSkuCode
tags: tags

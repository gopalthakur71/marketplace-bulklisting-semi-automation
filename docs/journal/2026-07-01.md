# Day Journal — 2026-07-01

Myntra Bulk-Listing Automation · **Phase-4 EC2 deploy, Stage 1.** Goal for the day: take
the image CI publishes to ECR and actually **run the Marigold Ops FastAPI app on an EC2
box**, reachable from a browser. **Result: achieved** — the app is live on a `t3.micro`,
pulled from ECR, serving on port 80, confirmed from both a laptop browser and a phone.

Getting there was a gauntlet of small infra failures plus one real code bug hiding in CI.
Every error we hit is catalogued below with root cause + fix, because most of them are the
kind that will recur on the next launch.

---

## Summary

- Followed `docs/runbooks/web-ec2-deploy-console.md` Stage 1 (create role → SG → launch with
  user-data → verify with `AUTH_DISABLED=1`).
- Hit **9 distinct failures** before the app rendered (see the error log). The headline one:
  **ECR `:latest` was stale** because **CI had failed on every run since the web-app merge** —
  a deprecated `TemplateResponse` call form that only *warned* on the local Starlette but is a
  hard `TypeError` on CI's newer (unpinned) Starlette.
- **Fixed the CI bug** (`d566304`), **verified against CI's actual stack** (fresh venv, Starlette
  1.3.1 → 57 passed), pushed → CI green → correct uvicorn image published → restarted the box →
  app live.
- Also improved the deploy runbook and added an **uploadable `userdata.sh`** (`23c94da`) so the
  CRLF/paste failure can't recur.

---

## What "done" looks like

On the box:
- `docker ps` → `COMMAND` is `uvicorn src.web.mai…` (not the old `python run.py`), ports
  `0.0.0.0:80->8080`.
- `curl -i http://localhost` → `HTTP/1.1 200`, `server: uvicorn`, Marigold Ops HTML.
- Browser + phone at `http://52.66.238.56/` → the Marigold Ops dashboard, logged in as `dev@local`.

Instance: `i-0add667d4cec224c6` (`listing-app`), `t3.micro`, ap-south-1b, public IP
**52.66.238.56** (changes on stop/start), SG `listing-app-sg` (`sg-06ceeb9a898378bb0`).

---

## Error log — everything we hit, in order

### E1 — SSH private key "permissions too open" (WSL)
- **Symptom:** `ssh -i listing-app.pem …` → `Permissions 0777 for 'listing-app.pem' are too
  open … bad permissions … Permission denied (publickey)`.
- **Root cause:** the key lived on `/mnt/e/...`, a Windows NTFS drive mounted in WSL as DrvFs,
  which forces everything to `0777`; `chmod` **silently doesn't stick** there.
- **Fix:** opened the same key from **Git Bash** instead (worked immediately). Alternative
  documented: copy the key into the WSL ext4 home (`~/.ssh/`) and `chmod 600` there.

### E2 — User-data never ran (instance #1)
- **Symptom:** `systemctl status listing-app` → *"Unit … could not be found"*; `docker
  --version` → *command not found*. Nothing was installed.
- **Root cause (first instance):** user-data didn't execute. We created a clean, LF-only
  `aws/ec2/userdata.sh` to upload directly (avoid CRLF paste mangling).

### E3 — `ERR_CONNECTION_REFUSED` in the browser
- **Symptom:** browser to the public IP → *refused to connect*.
- **Diagnosis:** **refused ≠ timeout.** Refused means the host was reached but **nothing is
  listening on port 80** (as opposed to a SG block, which *times out*). Confirmed the container
  wasn't up.

### E4 — No outbound internet → `dnf` couldn't install Docker (instance #2)
- **Symptom:** relaunched; user-data *still* didn't produce Docker. `ping www.google.com` →
  100% packet loss.
- **Root cause:** the **security group's outbound (egress) rule** had been removed/altered.
  SGs are **stateful**, so inbound SSH still worked (the reply is allowed), but **new outbound
  connections** (`dnf`, `docker pull`) were dropped → `dnf install -y docker` failed → with
  `set -euo pipefail` the whole user-data script aborted. This was the real reason user-data
  "didn't run."
- **Fix:** re-added **Outbound: All traffic → `0.0.0.0/0`**. `ping` then succeeded.

### E5 — Container up but serving nothing (`curl localhost:80` refused)
- **Symptom:** after installing Docker manually and starting the service, `docker ps` showed
  the container "Up", **but** `curl -i http://localhost` → *Failed to connect to port 80*.
- **Root cause:** `docker ps` `COMMAND` column read **`python run.py`** — the **CLI batch
  entry, not the uvicorn web server.** Nothing listened on 8080 inside the container, so the
  `80->8080` mapping forwarded to a dead port. The running image was the **wrong (old) one.**

### E6 — ECR `:latest` was stale (the headline problem)
- **Symptom:** the box faithfully pulled `:latest`, but `:latest` had `CMD python run.py`,
  from before the FastAPI app existed. The repo `Dockerfile` correctly says
  `CMD ["uvicorn", "src.web.main:app", "--host", "0.0.0.0", "--port", "8080"]` (commit
  `8444761`), so the image in ECR was out of date.
- **Root cause:** **CI had failed on every run since the web-app merge.** GitHub Actions
  showed run #1 (`c03a6e5`, Jun 29) green — that built the old placeholder image now stuck as
  `:latest` — and **runs #2–#6 (Jun 30, the `ca42958` merge and after) all red.** Because
  `build-and-push` has `needs: test`, a red `test` job means the image is never rebuilt, so
  `:latest` froze at the last green build.

### E7 — The actual CI bug: `TypeError: unhashable type: 'dict'`
- **Symptom:** the failing job was **`test`** (deploy gate), on
  `tests/web/test_fix.py::test_fix_upload_shows_buckets`, raising `TypeError: unhashable type:
  'dict'` deep in Jinja2's template cache.
- **Root cause:** the **deprecated `TemplateResponse(name, context)` positional form** (used
  **8×** across `pages.py`, `generate.py`, `fix.py` — the exact nit logged back on the web-app
  build). It worked locally because the dev machine has **Starlette 0.38.6**, which only emits
  a `DeprecationWarning`. **CI installs deps unpinned**, so it pulled **Starlette 1.3.1**, where
  the old form is a hard error: it treats the template-name string as the `request` and the
  **context dict as the template name** → Jinja tries to use a dict as a cache key → crash.
  *This is why tests "passed locally" but CI was red — a silent local/CI dependency drift.*
- **Fix (`d566304`):** migrated all 8 calls to the current `TemplateResponse(request, name,
  context)` signature (and dropped the now-redundant `"request"` key from the context dicts).
- **Verification (did it properly):** local tests pass on any version, so they don't *prove*
  the fix. Built a **fresh venv with the latest unpinned deps** — resolved **Starlette 1.3.1 /
  FastAPI 0.138.2 / Jinja2 3.1.6**, i.e. CI's exact stack — and ran the full gate:
  **57 passed in 121s.** Fix confirmed against the environment that was actually failing.

### E8 — Browser still blank after the fix (transient)
- **Symptom:** once the correct image was running (`curl localhost` → 200), the **browser** at
  the public IP still timed out for a while.
- **Diagnosis / resolution:** purely the network path (app was healthy on localhost). It
  started working — on both browser and phone — after re-saving the SG inbound rules and a
  fresh browser load. Most likely a **cached failed connection** in Chrome plus the SG rule
  re-apply. Noted the isolation ladder for next time: `curl` from the laptop (bypass browser) →
  re-check SG port-80 source → confirm current public IP → test on phone/mobile data (rule out
  ISP blocking port 80).

### E9 — Console role/policy flow didn't match the runbook
- **Symptom:** Step 1's "create the inline policy while creating the role" didn't line up with
  the current console.
- **Fix:** had to **create the role**, **create the policy separately**, then **attach** it.
  Rewrote runbook Step 1 as explicit **A (create role) / B (create policy) / C (attach)**.

---

## Changes committed today

- **`d566304`** `fix(web): migrate TemplateResponse to (request, name) signature` — the CI
  unblocker. 8 calls across the three routers.
- **`23c94da`** `docs(deploy): uploadable userdata.sh + EC2 runbook fixes`:
  - `aws/ec2/userdata.sh` — LF-only, upload directly as EC2 user data (no CRLF-in-shebang trap).
  - `.gitattributes` — force `*.sh` to LF so a Windows checkout can't re-corrupt it.
  - `web-ec2-deploy-console.md` — Step 1 A/B/C role flow; a CRLF warning; a "user-data never
    ran" recovery path (cloud-init logs + manual re-run); the `aws sts get-caller-identity`
    role check.

After pushing `23c94da`, **CI run #7 went green** and published a correct uvicorn `:latest` to
ECR; `sudo systemctl restart listing-app` on the box pulled it and the app came up.

---

## Lessons / decisions

- **`refused` vs `timeout` is a fast diagnostic.** Connection *refused* = reached the host,
  nothing listening (app/port problem). *Timed out* = dropped in transit (SG/NACL/route/ISP).
- **Security groups are stateful** — inbound SSH working tells you nothing about outbound.
  A broken egress rule breaks `dnf`/`docker pull` while SSH still works.
- **`docker ps` COMMAND column is the truth** about which image is really running. `python
  run.py` vs `uvicorn …` instantly told us `:latest` was stale.
- **Unpinned `requirements.txt` caused a silent local/CI split** — this whole deploy detour
  traces back to it. **Follow-up (agreed, not yet done): pin `requirements.txt`** to the
  resolved versions and re-verify, so CI and local can't drift again.
- **Verify a fix against the environment that failed, not the one that passed.** Local Starlette
  0.38.6 would have "confirmed" a fix that might not work on CI's 1.3.1; the fresh-venv run is
  what actually proved it.

---

## Open / next

- ⚠️ **SECURITY — do before leaving it up:** inbound **port 80 is `0.0.0.0/0`** while the app
  still runs with **`AUTH_DISABLED=1` (no login)** — anyone on the internet is `dev@local` and
  can drive the tool + touch S3. **Narrow port 80 back to "My IP"** until Stage 2 auth exists.
  Also can remove the unused **8080** and **All ICMP** inbound rules.
- **Pin `requirements.txt`** (root cause of E6/E7) and re-run the suite.
- **Stage 2 (real auth):** build `/login` + `/auth/callback` + `/logout`, register the prod
  callback URL in Cognito, drop `AUTH_DISABLED`, read config from SSM/Secrets via the instance
  role. Only then is `0.0.0.0/0` safe.
- **Investigate why user-data didn't run even from an uploaded file** (E2) — the LF `userdata.sh`
  + the E4 egress fix should cover it next launch, but confirm via `/var/log/cloud-init-output.log`.
- **TLS / stable URL** (deferred): the public IP changes per stop/start and HTTP is unencrypted.

---

## Where things stand at end of 2026-07-01

- **Stage 1 EC2 deploy = DONE.** Marigold Ops FastAPI live on `i-0add667d4cec224c6`, pulled from
  ECR, reachable publicly on port 80 (browser + phone), running `AUTH_DISABLED=1`.
- **CI is green again** and publishing correct images (the `TemplateResponse` fix restored the
  `test` → `build-and-push` chain).
- `main` advanced to **`23c94da`** (+ this journal).
- **Deploy mechanism proven:** boot/restart re-pulls `:latest` = redeploy.
- **Immediate next:** lock down port 80, pin deps, then Stage 2 auth.

---
---

# Session 2 (same day) — Stage 2: real auth (Cognito hosted-UI login)

Second working session on 2026-07-01. Goal: build the **real login round-trip** the
Stage-1 box has been running without (`AUTH_DISABLED=1`), so the app can eventually
enforce auth. **Result: achieved for the app code** — `/login`, `/auth/callback`,
`/logout` built, reviewed, merged to `main`, CI green. Auth is **not yet enforced in
prod** (that's the deploy runbook, deliberately a separate step).

## Scope decided (brainstorm)

Three framing choices locked before any code:
- **How far:** app code + tested, *through* deploy-to-EC2 wiring — but **keep My-IP-only,
  no TLS, no public** (TLS/stable-URL/public stays a later chunk).
- **Sessions: re-login-on-stale.** Started leaning toward silent refresh; on seeing the
  added machinery (refresh cookie + middleware) we dropped it — **no refresh tokens**,
  bump Cognito id-token validity to ~8h instead. This collapsed the design (no
  middleware; the existing `get_user`/`verify_jwt` path stays untouched).
- **No Elastic IP.** Cost concern raised → clarified the 2024 AWS pricing (a running
  instance already pays for one public IPv4; an EIP attached to it is $0 extra, only
  billed while the instance is *stopped*). Chose: keep the instance running, re-register
  the callback on the rare stop/start; add an EIP later only if restarts get frequent.

Spec + plan: `docs/superpowers/{specs,plans}/2026-07-01-web-auth-stage2-cognito-login*`.

## What shipped

- **`src/web/oauth.py`** — stdlib `urllib` helpers (no new deps): `authorize_url`,
  `exchange_code` (client secret via HTTP Basic; **injectable `http`** so tests never
  hit the network), `logout_url`.
- **`src/web/routers/auth_routes.py`** — `GET /login` (random `state` → short-lived
  cookie → hosted UI), `GET /auth/callback` (verify `state` **before** exchange,
  reject code-less callback, exchange code, `verify_jwt` the id_token, set `id_token`
  cookie w/ `max_age`≈8h), `GET /logout`.
- **`src/web/main.py`** — `AuthError` handler now **redirects** instead of 401 JSON:
  `302 → /login` for navigations, `HX-Redirect: /login` (200) for HTMX.
- **`COOKIE_SECURE` setting** — default **off** (no TLS yet); the cookie is
  `HttpOnly; SameSite=Lax` but **not `Secure`**, acceptable only because access stays
  My-IP-only. Flip on when TLS lands, zero code change.

## How it was built (subagent-driven)

5 tasks, one implementer subagent each (cheap tier for transcription tasks, mid-tier
for the route integration), a spec+quality review after every task, then a final
whole-branch review on Opus. Two review-driven fix passes, both test-backed:
- **Task 3 review** (Important ×2): a code-less callback (`?state=…` with no `code`,
  e.g. Cognito `error=access_denied`) burned a network round-trip before failing →
  now short-circuits to `/login`; and the logout test didn't assert the cookie was
  actually cleared → it does now.
- **Final review** (Minor polish): `id_token` was a **session cookie** (died on browser
  close despite the ~8h token) → gave it `max_age`≈8h to match, so re-login-on-stale
  actually stays infrequent; and `logout_url`'s site-root derivation made robust via
  `urlsplit`/`urlunsplit` instead of a brittle `urljoin`.

Final Opus verdict: **ready to merge, no Critical/Important.** Security pieces confirmed:
state-before-exchange CSRF guard, code-less rejection, post-exchange token verification,
`HttpOnly`/`SameSite` cookies, **no open redirect** (callback only ever redirects to the
literals `/` or `/login`), **no token leakage** (id_token only in the HttpOnly cookie).

## Merge + CI

- Branch `feat/web-auth-stage2` (7 commits) → merged no-ff to `main` (**`848a7fd`**),
  pushed. **72/72 repo tests pass** on the merged result.
- CI `ci-cd` run for `848a7fd` → **success** (run 28501143768). Fresh `:latest` published
  to ECR carrying the auth routes.

## Where Stage 2 stands at end of day

- **App code = DONE + merged + CI green.** The previously-deferred `/auth/callback` login
  flow now exists.
- **Auth is NOT live yet.** Merging shipped the code; the EC2 systemd unit still sets
  `AUTH_DISABLED=1`, so runtime behavior is unchanged. Turning it on = the new
  **"Stage 2 — enable real auth"** section in `docs/runbooks/web-ec2-deploy-console.md`:
  set Cognito id-token validity ~8h + register `http://<EC2_IP>/auth/callback` &
  sign-out `http://<EC2_IP>/`, set SSM `cognito_redirect_uri` (+ confirm the other
  `cognito_*` params and instance-role read perms), drop `AUTH_DISABLED` from systemd,
  redeploy, verify a login round-trip. **Keep My-IP-only — no `0.0.0.0/0` until TLS.**
- **Next session (handed off):** run those EC2 deploy steps; then the TLS + stable-URL +
  public chunk.
- `main` advanced to **`848a7fd`** (+ this journal).

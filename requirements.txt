# Fully pinned for reproducible installs — local and CI install the identical set.
#
# WHY PINNED: on 2026-07-01 an unpinned build let CI resolve a much newer Starlette
# (1.3.1) than the dev machine (0.38.6), turning a deprecated TemplateResponse call
# into a hard TypeError. CI went red for days and ECR :latest froze. Pinning the whole
# resolved set (incl. transitive deps like starlette, pulled via fastapi) prevents that.
#
# Versions below == the environment verified green (57 tests, Starlette 1.3.1 / FastAPI
# 0.138.2 / Jinja2 3.1.6). To upgrade deliberately: recreate a venv, `pip install` the
# direct deps unpinned, run the full suite, then `pip freeze` back into this file.

# ── Direct dependencies ──
boto3==1.43.38
fastapi==0.138.2
google-generativeai==0.8.5
httpx==0.28.1
jinja2==3.1.6
openpyxl==3.1.5
pandas==3.0.3
Pillow==12.2.0
pytest==9.1.1
python-jose[cryptography]==3.5.0
python-multipart==0.0.32
PyYAML==6.0.3
requests==2.34.2
uvicorn[standard]==0.49.0

# ── Transitive dependencies (pinned for reproducibility) ──
annotated-doc==0.0.4
annotated-types==0.7.0
anyio==4.14.1
botocore==1.43.38
certifi==2026.6.17
cffi==2.0.0
charset-normalizer==3.4.7
click==8.4.2
colorama==0.4.6
cryptography==49.0.0
ecdsa==0.19.2
et_xmlfile==2.0.0
h11==0.16.0
httpcore==1.0.9
httptools==0.8.0
idna==3.18
iniconfig==2.3.0
jmespath==1.1.0
MarkupSafe==3.0.3
numpy==2.5.0
packaging==26.2
pluggy==1.6.0
pyasn1==0.6.3
pycparser==3.0
pydantic==2.13.4
pydantic_core==2.46.4
Pygments==2.20.0
python-dateutil==2.9.0.post0
python-dotenv==1.2.2
rsa==4.9.1
s3transfer==0.19.0
six==1.17.0
starlette==1.3.1
typing-inspection==0.4.2
typing_extensions==4.15.0
tzdata==2026.2
urllib3==2.7.0
watchfiles==1.2.0
websockets==16.0
